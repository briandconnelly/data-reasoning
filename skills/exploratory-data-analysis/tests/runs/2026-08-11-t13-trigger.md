# T13 — entity profile, two-skill prompted dispatch

- Wave: `artifacts/2026-08-11-t13-t14-trigger-prereg.md` (preregistered before any arm; revised once after cross-model design review, still before any arm).
- Model: `claude-opus-5`, fresh `claude -p` process per rep via `tests/run_trigger.py`.
- Prompt: "Tell me about the Northgate Clinic account." — no fixture, as preregistered.
- Catalog: both descriptions stated in the first user message, parsed live from each `SKILL.md` frontmatter at `4efdeec`; order counterbalanced (reps 1/3/5 eda-first, reps 2/4 hda-first).
- Scorer: `run_trigger.py` detector (`ROUTE:` token parse), then manual review of every first text against the transcript by the wave operator.
- Arm archives: `artifacts/2026-08-11-t13-t14-trigger/t13-rep*.{jsonl,detect.json,stderr}`; dispatch-prompt digests in the `manifest-t13-*.json` files there.
- Wording fixed: `git diff` over both `SKILL.md` files empty across the wave; both description SHA-256s identical in every manifest.

## Assertions

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| T13-A1 | selected = exploratory-data-analysis and ≠ hypothesis-driven-analysis, per rep | PASS 5/5 | `summary.tsv` rows t13 rep1–rep5, `route=eda`, `valid=True`; token text verified per rep in each `t13-rep*.detect.json` `first_text`/transcript |

Total: 5/5.
Recorded, not scored: every rep consulted only `exploratory-data-analysis/SKILL.md` (`read_eda_skill=True`, `read_hda_skill=False` in all five rows); every rep wrote an `exploration-log.md` in its scratch directory — profile-route ceremony executed without data, recorded as executed evidence only.
The `scenarios.md` observation "no leads chased" holds in all five transcripts, trivially: no data existed to chase leads in, so this carries no evidential weight for route behavior.

## Instrument check

c-eda → `eda`, c-hda → `hda`, c-none → `none` (smoke tests, archived in the same directory); the excluded T13 rationale canary declared `ROUTE: exploratory-data-analysis` with a rationale engaging the description's constructs — "a named entity whose story is wanted, with no effect to explain and no claim to adjudicate" — and the manual reading matched the detector.

## Verdict

Cell outcome row 1: 5/5 expected — no collision detected at screening precision.
Screening precision means exactly that: 5/5 has a two-sided 95% Clopper–Pearson interval of roughly 0.48–1.00, so this result does not establish a stable pass rate.

## Unpreregistered observations

- Reps 1, 3, and 5 — all three eda-first reps — emitted the ROUTE token only after one to three initial Bash calls ("I'll start by looking at what data is available."), a deviation from the dispatch instruction. Each token is present, unambiguous, and manually verified against the transcript, so classification is unaffected. That the deviation aligns perfectly with catalog order is noted; at n=5 it is a coincidence-compatible pattern, not a finding.
- Arms invoked Bash although `--allowedTools` listed only Read/Write/Glob/Grep: that flag gates approval, not tool availability. The operator's user-global configuration was present in every arm, identically, as disclosed in the preregistration.

## Per-run metadata

- Durations (s): 85.6 / 74.4 / 82.7 / 83.4 / 90.7. Tool calls: 8 / 6 / 7 / 8 / 8. All exit 0, all transcripts valid.
- Voids: none in this cell.

Evidence: `artifacts/2026-08-11-t13-t14-trigger/` (transcripts, detector output, manifests, `summary.tsv`).
