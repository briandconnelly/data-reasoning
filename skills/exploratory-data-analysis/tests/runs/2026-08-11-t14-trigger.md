# T14 — named effect on the same entity, two-skill prompted dispatch

- Wave: `artifacts/2026-08-11-t13-t14-trigger-prereg.md` (preregistered before any arm; revised once after cross-model design review, still before any arm).
- Model: `claude-opus-5`, fresh `claude -p` process per rep via `tests/run_trigger.py`.
- Prompt: "Why did the Northgate Clinic account's spend drop last quarter?" — no fixture, as preregistered.
- Catalog: both descriptions stated in the first user message, parsed live from each `SKILL.md` frontmatter at `4efdeec`; order counterbalanced (reps 1/3/5 eda-first, reps 2/4 hda-first).
- Scorer: `run_trigger.py` detector (`ROUTE:` token parse), then manual review of every first text against the transcript by the wave operator.
- Arm archives: `artifacts/2026-08-11-t13-t14-trigger/t14-rep*.{jsonl,detect.json,stderr}`; dispatch-prompt digests in the `manifest-t14-*.json` files there.
- Wording fixed: `git diff` over both `SKILL.md` files empty across the wave; both description SHA-256s identical in every manifest.

## Assertions

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| T14-A1 | selected = hypothesis-driven-analysis and ≠ exploratory-data-analysis, per rep (symmetric: `none` does not pass) | PASS 5/5 | `summary.tsv` rows t14 rep1-r2 and rep2–rep5, `route=hda`, `valid=True`; token text verified per rep in each `t14-rep*.detect.json` `first_text` |

Total: 5/5.
Recorded, not scored: every valid rep consulted only `hypothesis-driven-analysis/SKILL.md`; every valid rep wrote an HDA-style ledger (`ledger-northgate-spend.md` or `ledger.md`) and none wrote an exploration log, so the `scenarios.md` observation "no exploration log is created" holds in all five — trivially, since no data existed for any route to execute on.

## Void and rerun

The original t14-rep1 hit the 600s timeout and was killed with no result event (`exit=-9`); per the preregistered gate it is void, excluded, and preserved in the archive (`t14-rep1.jsonl`, valid=False).
Its single disclosed rerun, `t14-rep1-r2`, completed in 115.2s and is the scored rep 1.
The voided partial transcript had also declared `ROUTE: hypothesis-driven-analysis` before timing out in a search for nonexistent Northgate data — it had only just read the ledger template and never wrote a ledger when the kill hit; this is consistent with, but not counted in, the scored result.

## Instrument check

c-eda → `eda`, c-hda → `hda`, c-none → `none` (smoke tests, archived in the same directory); the excluded T14 rationale canary declared `ROUTE: hypothesis-driven-analysis`, reasoning from the named effect and rival explanations ("churned line items, price change, seasonality, billing-period artifact"), and the manual reading matched the detector.

## Verdict

Cell outcome row 1: 5/5 expected — no collision detected at screening precision.
Screening precision: 5/5 spans roughly 0.48–1.00 (two-sided 95% Clopper–Pearson), so this is not evidence of a stable pass rate.
Known non-independence, preregistered: this prompt is near-identical to the counter-example phrase the EDA description itself carries, so a pass here is weak evidence about the exclusion clause in general.

## Per-run metadata

- Durations (s): 115.2 (rep1-r2) / 125.1 / 134.7 / 68.6 / 86.0. Tool calls: 11 / 8 / 8 / 5 / 8. All scored reps exit 0 and valid.
- Voids: one (original rep1, timeout), disclosed above and preserved in the archive.

Evidence: `artifacts/2026-08-11-t13-t14-trigger/` (transcripts, detector output, manifests, `summary.tsv`).
