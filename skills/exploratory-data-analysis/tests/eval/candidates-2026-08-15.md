# Candidate descriptions — 2026-08-15 widening (issue #17)

Derived, not hand-typed: `make_candidates_2026_08_15.py` builds each file from `frozen-2026-08-11-treatment.txt` and asserts lengths and the intact exclusion tail.
Regenerating must be byte-identical; these files are frozen and never edited.

- `B-compressed` (983): the shipped text minus `, to confirm, never to conclude from` — the compression step issue #17 requires to ship separately, measured on its own.
- `C1` (994): B plus an activity-naming opening — "any request about a named entity … that asks what is going on with it, in whatever words". One edit against B.
- `C2` (1021): B plus two generic-verb exemplars in the example list. One edit against B.
- `C3` (1021): C1's opening plus a phrasing-does-not-matter clause replacing part of the exemplar list. Two edits against B; if it wins, attribution between its mechanisms is unmeasured.

All four hold the exclusion tail byte-identical to the shipped text.
The gates these candidates must clear are stated once, in `2026-08-15-widening-prereg.md`, and are not restated here.
