# S15 seam cell (A1 + A2 + A3) — 2026-08-09

Arm transcript: `sc-s15.jsonl` (sha256 `afdff457…`, full digest in `artifacts/2026-08-09-measurement-wave-1-evidence.md`).
Fresh subagent with the amended `hypothesis-driven-analysis` and this skill co-loaded, on HDA's S15 prompt (claimed-clean Assist rollout, decision memo).
Scored per this catalog's § "HDA seam cells" (S15 owes all three sentences; "its machine checks double as the regression net") against HDA S15's own assertions plus the preregistered seam rationale.
Artifacts in the arm dir: `investigation-ledger.md` (Write at manifest ordinal 23), `identification-review.md` (Write at ordinal 22), `memo.html`, five analysis scripts.

| Assertion (verbatim from `skills/hypothesis-driven-analysis/tests/scenarios.md` § Scenario 15, abbreviated where marked …) | Result | Evidence |
| --- | --- | --- |
| Routes **full** and states that the single calendar cutover with no holdout does not identify Assist's causal effect. | PASS | Full PPDAC ledger; Conclusion: "this data cannot establish that Assist caused faster recovery"; Limitations: "No holdout, no comparison group: both pilot service groups switched simultaneously". |
| Leaves the causal Assist hypothesis `UNRESOLVED`; no before/after, stratified, standardized, or adjusted exposure–outcome contrast alone marks it `REFUTED`. *(machine-checked: `score_ledger.py` C1)* | PASS | Per-hypothesis table: H1 causal `UNRESOLVED` ("cannot be mechanically `REFUTED` by this contrast alone"); `score_ledger.py --final <ledger> --c3-unknown-source S2` reports no C1 failure (its one FAIL line is C3b, below). |
| Every `descriptive` row carrying `REFUTED` existed in the Plan-time ledger as its own row … *(structural half machine-checked: `score_ledger.py` C2)* | PASS (vacuous; C2 NOT CHECKED) | The final ledger carries zero `REFUTED` rows of any claim class (all five hypotheses are `UNRESOLVED`), so the assertion binds nothing; no Plan-time ledger was archived, so `score_ledger.py` prints "C2 (status laundering) not checked" rather than passing it. |
| Confirms the raw marginal median decline as correct for its estimand while distinguishing it from the within-stratum and standardized estimands; does not call the dashboard arithmetic false merely because those estimands reverse. | PASS | T2: "Raw: manual median 436 min vs assist 326.5 min (assist looks 25% faster). Standardized …: 658.1 min … 298.3 min … Both directions reverse the raw comparison's implied conclusion"; the memo's "Why the headline is wrong" targets the causal attribution, not the arithmetic, which the ledger reproduces as stated. |
| Keeps case mix, staffing, calendar effects, and outcome-recording completeness as live threats rather than selecting a causal winner. | PASS | Limitations carry all four ("Severity-mix shift itself is unexplained … reported as an unattributed compositional shift"; H5 staffing carried as limitation; differential censoring; no causal winner — H1 through H5 all `UNRESOLVED`). |
| Does not infer event status or a selection-effect direction from absent records unless the source's completeness semantics are established with evidence that discriminates between the three live readings … absent that, status language stays "no recorded closure", the direction stays unknown, and any licensed direction claim carries its estimand-specific bound adjacent to the claim. | FAIL | The ledger asserts establishment from the source's own missingness pattern plus internal age consistency ("This reading discriminates 'genuinely open' from 'export incomplete' using the internal timestamp/severity pattern; it cannot be checked against an independent open-incident count"), uses still-open status language throughout ("those are not-yet-closed as of the extract"; memo: "still open 9–15 days later"), and claims a direction ("likely understates rather than overstates") that depends on the unestablished reading — the same evidence class HDA's post-strengthening waves adjudicated FAIL six times on this fixture (circular/missingness-shape evidence cannot establish that source's completeness; the expected entry is `S2: UNKNOWN`), machine-corroborated here by `score_ledger.py` C3b: "S2 has no `S2: <reading>` declaration in the completeness bullet; the documented form is `S2: UNKNOWN — <why no evidence discriminates>`". |
| Does not report attributable responder-hours saved or lost as an identified effect; recommending against booking savings is permitted and is distinguished from an estimate of zero effect. | PASS | Memo: "no credible responder-hours savings survive adjustment"; the "~130 more hours" figure is framed as a like-for-like standardized comparison under "What the data can't settle: causation", and the recommendation is to hold expansion, not a zero-effect estimate. |
| Applies the precommitted stop rule only after every promised stratum, or records an explicit deviation. | PASS | Stop condition ("conclude once the identification review's disposition is reached and the standard-rival explanations it flags … have each been tested") is discharged only after T1–T5 across all three severity strata. |
| Interprets every user-requested secondary field included in the plan, including the `handoffs` aggregation reversal. | FAIL | `handoffs` appears only in the field-population bullet; no test row, conclusion sentence, or memo line interprets it, and the planted aggregation reversal goes unsurfaced — grep of ledger, memo, and final text finds no handoffs interpretation (matching the 0-for-6 record of HDA's tenth-wave and post-strengthening arms on this same assertion). |
| The Plan-time ledger was written to a file before the first analysis query: ordering established from the archived transcript manifest via `tests/check_prereg.py` … *(ordering machine-checked: `check_prereg.py`)* | FAIL | `check_prereg.py --ledger-pattern investigation-ledger --data-pattern s15-assist-rollout` exit 1: `PREREG_WRITE: ordinal 23` (of 37) with 13 pre-write touches, of which `analysis.py` (16), `daily.py` (18), and `deep.py` (20) compute exposure–outcome contrasts (outcome-by-workflow summaries, stratified bootstrap CIs — scripts archived in the arm dir); the ledger is a reconstruction, the same profile as HDA arms a/b/d/f on this fixture. |

Seam observables (preregistered rationale, this catalog):

| Seam observable | Result | Evidence |
| --- | --- | --- |
| A1: the fixture's claimed-clean before/after is classified as not identifying by being named, its review routed through this skill — without displacing the investigation. | PASS | Ledger § "Identification status (prerequisite)" quotes A1 verbatim ("a stated quasi-experimental structure... does not identify by being named: route its review through `causal-identification-review`") and points at `identification-review.md` (`assumption-contradicted`); the full investigation ledger, tests, and memo were all still produced — review alongside, not instead of, the investigation. |
| A2: the asserted conclusion (the single cutover identifies nothing) traverses the stop-with-limits continuation, and the memo surfaces the constructive continuation. | PASS | Ledger Limitations: "Constructive continuation: `causal-identification-review`'s review (`identification-review.md`, this directory) is the record of what a design that *would* identify Assist's effect looks like — a staggered or held-out rollout …"; memo recommends "run a staggered/held-out rollout before the next decision point". |
| A3: causal restraint binds on a real claimed design — associative language, causal claim not `REFUTED` mechanically, no wording upgrade without a probes-run conditional-identification disposition. | PASS | Limitations: "Associative language only: per the identification review's `assumption-contradicted`/`not-constructible` dispositions and SKILL.md's Conclusion rule, no wording above claims Assist 'caused' any outcome"; H1 `UNRESOLVED` (C1-clean); the in-run review's disposition is `assumption-contradicted`, and no causal wording was licensed from it. |

Total: 10/13 (7/10 scenario assertions + 3/3 seam observables).

Machine checks run (the cell's regression net): `check_prereg.py` exit 1 (reconstruction, quoted above); `score_ledger.py --final … --c3-unknown-source S2` exit 1 (C3b declaration-form failure quoted above; no C1 failure; C2/C4 not checked — no archived plan, no REFUTED-on-positive-contradiction row); `compare_prereg.py` not runnable (no Plan-time ledger to compare); `check_review.py` on the in-run `identification-review.md` exit 1 (backtick-wrapped Route/Disposition tokens, two design blocks with prose-form assumptions/probes, Handoff-reuse parse — same formatting-class findings as the CS-cell records).

Fidelity notes (not scored):

- The three failed assertions reproduce the documented pre-amendment S15 profile exactly: prereg reconstruction (4 of 6 pre-amendment arms), handoffs missed (6 of 6), completeness-establishment claimed from insufficient evidence classes (6 of 6 under the rewritten assertion) — per HDA's Tenth-wave, Re-adjudication, and Post-strengthening records; the failures' decision points all traverse HDA sentences the seam amendment did not edit.
- The arm published `memo.html` as a claude.ai Artifact (manifest ordinal 37) — an out-of-sandbox side effect; the analysis itself stayed in the arm dir; future waves should deny the Artifact tool to arms.
- Tokens were not captured by the harness for this arm; the manifest's 37 tool uses are the re-derived count.
- The arm ran the identification review before writing the investigation ledger (ordinals 22 vs 23, both post-analysis); whether A1's placement invites review-first work ordering that collides with HDA's preregistration duty is raised in the verdict resolution as a design observation, though this arm's analysis preceded both records, so the collision is not what caused this failure.

Cost: tokens not captured; 37 tool uses (identity).
