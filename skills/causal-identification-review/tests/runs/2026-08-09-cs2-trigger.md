# CS2 trigger — 2026-08-09

Arm transcript: `sc-cs2.jsonl` (sha256 `e537ada8…`, full digest in `artifacts/2026-08-09-measurement-wave-1-evidence.md`).
Skill catalog: all three descriptions loaded, skills at c5f4755.
Scored against `tests/scenarios.md` § CS2.

| Assertion (verbatim) | Result | Evidence |
| --- | --- | --- |
| `causal-identification-review` is selected over both other skills. | PASS | Text block 1 (pre-tool): "This is a \"how could we ever measure this causal effect without randomization\" question — exactly what causal-identification-review is for (candidate designs, their identifying assumptions, and probes to test them). Loading that skill now." — neither other skill activates anywhere in the transcript. |
| The skill begins its identification-review record (per the template, once written) rather than answering inline — a started record is observable as a written artifact naming the causal question and beginning the assumption/design fields, whatever route it eventually resolves to. | PASS | Manifest ordinal 5: `Write` → `sc-cs2/price-increase-churn-identification-review.md`; the archived record carries the Question block (counterfactual contrast, estimand, `UNSTATED` assignment mechanism) and four Design blocks with assumptions and probe tables. |

Total: 2/2.

Fidelity notes (not scored):

- CS2 is scored on activation and record-start only (catalog: "not on the record reaching a route or a disposition"), but the machine gate was still run on the record it produced: `check_review.py` exits 1 with a genuine closed-set violation — the synthetic-control block's disposition reads "not-constructible if fewer than a handful of untreated comparable markets/products exist; otherwise unresolved pending the fit and placebo-in-space probes.", a conditional compound where the contract requires one closed-set token, which also breaks the Handoff reuse check.
- That finding is outside CS2's preregistered scope (disposition depth belongs to CS4) and is carried in the verdict resolution as a record-discipline observation, not scored here.
- The record's `UNSTATED` assignment-mechanism handling and its "uniform simultaneous increase defeats every design above" caveat are route-appropriate for a prompt with no rollout facts.

Cost: 47,165 tokens (harness-reported, n=1); 5 tool uses (identity).
