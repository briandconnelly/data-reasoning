# Design review record — 2026-09-15 remediation wave

PROTOCOL step 3: a cross-model review of the preregistration, fixtures, harness, checker change, and changed sentences together, before any arm ran.
Reviewer: Codex (`codex_consult`, reasoning effort high, read-only, job `0c9e738f476544629eccfc6ff17d8bc2`, 2026-09-15 17:32–17:37 UTC).
Its verdict on the first draft: "Do not run the arms yet."
Every finding is listed with what was done; the preregistration was rewritten before the canaries.

| # | Finding | Severity | Disposition |
| --- | --- | --- | --- |
| 1 | The repository stays reachable to both arms: `--add-dir` named the repo, the tests-tree prohibition was prompt text only, and a baseline could read the skill. | high | **Fixed.** `run_arm.py` stages the fixture and, for a skill arm, a standalone copy of the skill without `tests/` into a directory outside the repo; that directory is the cwd and the only `--add-dir`; the prompt names only staged paths. Bash is not a sandbox, so every tool call is scanned afterwards for the repo path, a foreign `SKILL.md`, or a `tests/` path, and a hit voids the arm. |
| 2 | `--allowedTools` grants permission but does not remove tools; the no-file arm would still be offered Write, Edit, Bash. | high | **Fixed.** The runner passes `--tools` for the built-in set and archives the `system` init event's tool list; the no-file arm's manifest must show Read, Glob, Grep only. |
| 3 | The reference class of 10 real and 10 non-regressions supplies an empirical prevalence, so "no defensible prior" was not established; the validator searched for words, not properties. | high | **Fixed.** The ledger states the strata were drawn to quotas fixed by the analyst and that the split is not a base rate; the validator requires that statement. The assertion now reads "no supported prior", the skill's term. |
| 4 | H1 was causal, so a careful arm could detour into the causal-posterior restriction instead of the degraded mode; the assignment fact does not establish measurement invariance. | medium | **Fixed.** H1 is now `descriptive` — the regression is real in served latency, whatever caused it — which is what the reference-class labels settle and what the ship/hold decision turns on. Identification basis `NONE` is then correct, not a gap. |
| 5 | DA-S3 could satisfy the precedence assertion by combining the degraded mode and the Numeric Policy loss rule, never touching the precedence paragraph. | high | **Fixed.** DA-S3 leaves the wave. A new cell, DA-S8, supplies a user-elicited prior range and loss range that both straddle the decision boundary with the sourced LR, so both sensitivities fire on supported inputs and only the precedence paragraph decides the label. DA-S8 is added to the decision-analysis catalog. |
| 6 | B10.5 asserted a March plan-change date the extract cannot establish (February has no entity billing row). | high | **Fixed.** `plan_changes.csv` dates the change (2026-03-01) independently; the validator checks billing's first new-plan month matches it; B10.5 scores the dated event. |
| 7 | B10.6's timing boundary contradicted itself (before exploration output vs before the first Read). | medium | **Fixed.** One boundary throughout: Frame-lite appears in response text before the first tool call that reads a fixture data file; reading the skill, its references, and listing the fixture directory are exempt; the identifier may be provisional until `accounts.csv` is read. |
| 8 | The checker accepted the no-prior sentinel with a `robust` or `loss-sensitive` verdict and with a belief-grade sweep. | high | **Fixed.** `check_decision.py` couples the sentinel to a `prior-sensitive` verdict and a `sensitivity-only` sweep; negative tests for each demonstrated case, plus an all-`none supported` evidence case, added. |
| 9 | Baseline vs current skill cannot attribute behavior to the edited sentence; a canary's citation is process evidence, not attribution. | high | **Fixed.** Every cell gains a pre-edit arm: the same harness stages the skill from `main` (`--skill-ref main`). Attribution is pre-edit vs post-edit on the added assertion; baseline vs skill answers the separate question of whether the skill is needed at all. Rationale is recorded as process evidence only. |
| 10 | The verdict table lacked precedence between rows and dispositions for shared failures, unscoreable records, and checker false acceptance. | medium | **Fixed.** The table is now per cell over the three arms, with an explicit precedence order (void, then regression, then the added-assertion outcome) and rows for shared catalog failures, unscoreable output, and checker false acceptance. |
| — | Non-blocking: split compound assertions; state accepted S9 completeness language; archive the command and startup inventory; the B10 fixture is four CSVs plus a README, not five CSVs. | — | All done: assertions are split into lettered subchecks; S9.4b accepts `UNKNOWN` completeness as a pass; `<name>.command.json` and `tools_at_startup` are archived; the fixture now has five CSVs plus a README and the preregistration says so. |

The reviewer confirmed the DA-S1 arithmetic: threshold odds 0.1 from a 10:1 loss ratio, LR 4.5, prior crossover odds 0.0222 (prior probability about 2.2%).
It also verified in memory that every generated CSV reproduces and that the ledger passes `check_record.py --final`.
