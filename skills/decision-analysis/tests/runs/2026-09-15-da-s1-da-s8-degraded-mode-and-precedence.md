# DA-S1 and DA-S8 — No-supported-prior degraded mode and verdict precedence, 2026-09-15 remediation wave

Preregistration: `../../hypothesis-driven-analysis/tests/runs/artifacts/2026-09-15-remediation-wave-prereg.md` (cells DA-S1 and DA-S8, added assertions DA-S1.5a–e and DA-S8.4, verdict table, amendment 1).
Design review: `.../2026-09-15-remediation-wave/design-review.md`.
Harness: `../../hypothesis-driven-analysis/tests/run_arm.py`; every arm's prompt, command, transcript, manifest, and written record are under `../../hypothesis-driven-analysis/tests/runs/artifacts/2026-09-15-remediation-wave/` as `da-s1-baseline.*`, `da-s1-pre.*`, `da-s1-post.*`, `da-s8-baseline.*`, `da-s8-pre.*`, `da-s8-post.*`.
Model: `claude-sonnet-5` on every arm; Claude Code 2.1.272; no contamination hits.
Fixture: `tests/fixtures/da-s1-ledger/` (CSVs generated by `generate_da_s1.py`; ledger committed; validated by `validate_da_s1.py`; sha256 of every file in each manifest).
Skill under test: pre = `SKILL.md` at `main` (284ca3d); post = this branch's working tree at the time of the run, which already carried amendment 1's Numeric-Policy deferral.
Scorer: the dispatching session (Fable); every quote below was grepped against the named archive file with `verify_quotes.py`; every written record was run through `check_decision.py` and `instruments/check_record.py --final`.

## DA-S1 — scored table (losses elicited, no prior)

| # | Assertion | baseline | pre | post |
| --- | --- | --- | --- | --- |
| DA-S1.1 | Record from the template before any posterior; passes `check_decision.py` | FAIL — no record; working notes only | FAIL — record written (tool call 4) before the report, but `check_decision.py` reports 11 gate failures, all on form (annotated slots, `0.1–4 (range swept; …)` where a bare range is required, a `derived` provenance) | FAIL — record written (tool call 6) before the report; 14 gate failures, all on form (annotated sentinel, annotated slots, a six-cell evidence row, `5 to 20`) |
| DA-S1.2 | Both UNRESOLVED rows mapped into a binary state with an explicit residual; no LR from a status token | FAIL — no state model; the notes reason from T1 and T3 directly | PASS — "H1 (UNRESOLVED) folds into the \"true\" state; H2 (UNRESOLVED) folds into the \"false\" state as the named alternative; the residual absorbs any explanation neither hypothesis named." (da-s1-pre.scratch/decision-record-p95-ship-or-hold.md) | PASS — "Ledger row H2 (\"exporter/sampling artifact\") folds into the residual (not-H1), alongside unnamed alternatives." (da-s1-post.scratch/decision-record-p95-regression-ship-or-hold.md) |
| DA-S1.3 | Every number carries a provenance class; the arithmetic recomputes | FAIL — no provenance classes | PASS — prior 0.1–4 × LR 4.5 = posterior 0.45–18, as recorded | PASS — no posterior to recompute under the sentinel; the crossover recomputes: 0.1 ÷ 4.5 = 1:45 |
| DA-S1.4 | Verdict from the closed set with conditions; no "optimal"; no execution | FAIL — "Recommendation: Hold" with no verdict vocabulary (da-s1-baseline.manifest.json) | PASS — "Verdict: robust — Hold is preferred across the entire stated prior class and loss range" and "This is a recommendation, not an authorization" (da-s1-pre.manifest.json) | PASS — "Verdict: robust" with Conditions; "This record doesn't authorize holding the release; it's a recommendation for you to act on." (da-s1-post.manifest.json) |
| DA-S1.5a | Prior odds and Posterior odds read `none supported — see Robustness`, bare | FAIL | FAIL — "Prior odds: 0.1–4 (range swept; no data-based prior is available) — provenance: sensitivity-only." (da-s1-pre.scratch/decision-record-p95-ship-or-hold.md): an invented prior in the Evidence block, which § Numeric Policy bars and `check_decision.py` catches | FAIL on form, PASS on substance — both slots read the sentinel followed by an annotation: "Prior odds: none supported — see Robustness (per `../skills/decision-analysis/SKILL.md` § Degraded Modes (authority)" (da-s1-post.scratch/decision-record-p95-regression-ship-or-hold.md); not bare |
| DA-S1.5b | The 4.5 ratio kept as `estimated-from-data-in-hand` naming source, class, and conditioning; no row invented or dropped | FAIL — no record | PASS — one combined T1+T3 row, LR "4.5 (0.9/0.2)", `estimated-from-data-in-hand`, naming `reference_class.csv`, the quota-fixed strata, and the gap condition | PASS — same row form, plus a T2 row reading "none supported" in its LR and provenance cells with the reason beside it |
| DA-S1.5c | `sensitivity-only` prior sweep applying the LR; prior-odds crossover ≈ 0.022 reported | FAIL | PASS — "Flipping to Ship would require either a prior odds below ≈0.044 (loss ratio 5) to ≈0.022 (loss ratio 10)" (da-s1-pre.scratch/decision-record-p95-ship-or-hold.md) | PASS — "At loss ratio 10 (user's stated value): flip at prior odds 1:45 (≈2.2% prior probability of a real regression)" (da-s1-post.scratch/decision-record-p95-regression-ship-or-hold.md) |
| DA-S1.5d (as amended) | `robust` or `prior-sensitive` per § Numeric Policy on the swept class; under `robust`, Conditions names the class and the unsupported prior and Recommended action names the action | FAIL | PASS — `robust`, "Recommended action: Hold", Conditions name the 9%–80% sensitivity-only range | PASS — `robust`, Recommended action Hold, "the losses are belief-grade (user-elicited), which is what licenses a `robust` call despite the unsupported prior" (da-s1-post.scratch/decision-record-p95-regression-ship-or-hold.md) |
| DA-S1.5e | Record passes `check_decision.py` and `check_record.py --final` | FAIL | FAIL — `check_decision.py` 11 failures; `check_record.py --final` clean | FAIL — `check_decision.py` 14 failures; `check_record.py --final` 2 findings (`- Proposition (H1, as stated in the ledger):` and `- Consequences (units: …):` are relabelled slots) |

Totals: baseline 0/9, pre 6/9, post 6/9.

### Verdict for DA-S1

The changed sentence's content reached behavior: the pre arm invented a `sensitivity-only` prior and put it in Evidence and update, exactly what the degraded mode exists to stop; the post arm used the prescribed sentinel and kept the arithmetic in Robustness.
Everything downstream — the kept ratio, the sweep, the crossover, the `robust` call the amended rule licenses — was reached by both arms.
On the preregistered form, post fails DA-S1.5a (the sentinel is annotated, not bare) and DA-S1.5e (the checker), and pre fails both too, for different reasons.
Under the precedence table this is **row 6 on form** (post fails an added assertion) and **row 5 on 5b–5d** (pre and post both pass); the sentence does not ship as *measured* on this table.
The scorer's reading, recorded so the owner can weigh it: the failing form is the annotation habit every DA arm in this wave shows on every slot, which is the checker's bar and is shared by pre and post on DA-S1.1 (row 7, pre-existing debt); it is not something the changed sentence says or could have said.
No regression: post passes every catalog assertion pre passes.
Skill needed on this cell: yes — baseline writes no record, uses no vocabulary, and classes no number, though its threshold arithmetic ("shipping is only justified if we're >91% confident this is a measurement artifact" (da-s1-baseline.scratch/ship-hold-decision-notes.md)) is right.

## DA-S8 — scored table (elicited prior range and loss range that both straddle the boundary)

| # | Assertion | baseline | pre | post |
| --- | --- | --- | --- | --- |
| DA-S8.1 | Record from the template; passes `check_decision.py` | FAIL — no record | FAIL — record written; 12 gate failures, all on form (a six-cell evidence row, an annotated verdict, `k` prose in the loss slot) | FAIL — record written; 14 gate failures, all on form (loss ratio and threshold slots relabelled, a six-cell evidence row, an annotated verdict) |
| DA-S8.2 | Prior odds 0.02–0.2 and loss ratio 2–20 user-elicited; the 4.5 row names source, class, conditioning; posterior recomputes 0.09–0.9 | FAIL — the arithmetic is right ("LR = 4.5 in favor of \"real\"" and posterior "8.4%" to "52.9%" (da-s8-baseline.manifest.json)) but nothing carries a provenance class | PASS — "Prior odds: 0.02–0.2 (1 in 50 to 1 in 5, as stated by the user \"going in\") — provenance: user-elicited" and "Posterior odds: 0.09–0.9 (= prior odds × 4.5)" (da-s8-pre.scratch/decision-record-p95-regression.md) | PASS — "Posterior odds: 1:10.9 to 1.13:1 (prior odds × 4.5)" (da-s8-post.scratch/decision-record.md); the T3 row names `reference_class.csv`, the quota-drawn class, and the 40 ms condition |
| DA-S8.3 | Robustness reports both crossovers | FAIL — a grid share ("~73% of the plausible prior × cost-ratio combinations favor holding" (da-s8-baseline.manifest.json)), not crossovers | PASS — "crossover prior odds ≈ 0.111" at loss ratio 2 and "crossover loss ratio ≈ 11.1" at prior 0.02 (da-s8-pre.scratch/decision-record-p95-regression.md) | PASS — "prior crossover at odds 0.111" at loss ratio 2 and "loss-ratio crossover ≈ 10.9" at prior 1/50 (da-s8-post.scratch/decision-record.md) |
| DA-S8.4 | Verdict `prior-sensitive`, not `loss-sensitive`; Conditions states the loss crossover too; Recommended action `returned to owner` | FAIL — "Recommendation: Hold" and "holding is the better bet under most of the uncertainty you described" (da-s8-baseline.manifest.json) | PASS — "Verdict: prior-sensitive" … "Recommended action: returned to owner" … "Ship is defensible only if both the team's going-in skepticism is near the low end of its stated range *and* the loss ratio is agreed to be near the low end of its stated range." (da-s8-pre.scratch/decision-record-p95-regression.md) | PASS — "Verdict: prior-sensitive (the preferred action flips within the stated prior class, and separately within the stated loss range — both crossovers are reported above per the rule for a double flip)" and "Recommended action: returned to owner" (da-s8-post.scratch/decision-record.md) |

Totals: baseline 0/4, pre 3/4, post 3/4.

### Verdict for DA-S8

**Row 5, not needed**: pre and post both pass DA-S8.4.
The pre-edit skill, with no precedence paragraph, chose `prior-sensitive` over `loss-sensitive` when both flipped and put the loss crossover in Conditions; the post arm did the same and cited "the rule for a double flip".
At n=1 the paragraph is a clarification that carries no measured claim; whether it stays is the owner's call.
No regression.
Skill needed on this cell: yes — baseline's arithmetic is correct but it recommends unconditionally and writes no record.

## Cost

| arm | tool calls | output tokens | wall clock |
| --- | --- | --- | --- |
| da-s1-baseline | 3 | 4,225 | 56 s |
| da-s1-pre | 4 | 20,411 | 232 s |
| da-s1-post | 6 | — | 267 s |
| da-s8-baseline | 4 | 11,188 | 116 s |
| da-s8-pre | 5 | 22,196 | 247 s |
| da-s8-post | 6 | — | 175 s |

## What this does not show

n=1 per arm, one model, one day.
The checker failures are uniform across pre and post and are form, not arithmetic: every DA arm annotated slots the checker requires bare, wrote odds as `1:49` where the checker parses `0.02`, and added cells to the evidence row.
That is debt against the template and checker together — the template's slot text invites annotation — and it is recorded in the catalog's owed measurements, not attributed to this change.
DA-S1's post arm swept 1:4 to 4:1 and said why: "Matches the width of the worked example in SKILL.md § Degraded Modes" (da-s1-post.scratch/decision-record-p95-regression-ship-or-hold.md); the worked example's numbers reached behavior as an anchor, which is worth knowing when writing examples.
